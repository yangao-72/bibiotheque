# Identifiants de la base PostgreSQL.
# Copiez ce fichier en `.env` pour les personnaliser.
# Sans fichier `.env`, docker compose utilise ces valeurs par défaut.
DB_NAME=bibliotheque
DB_USER=bibliotheque
DB_PASSWORD=bibliotheque
